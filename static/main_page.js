const force_page = () => {
    window.location.replace("/Force")
}

const energy_page = () => {
    window.location.replace("/Energy")
}

const shm_page = () => {
    window.location.replace("/SHM")
}

